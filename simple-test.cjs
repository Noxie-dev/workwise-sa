// Simple test to check if Firebase Admin SDK is working
const admin = require('firebase-admin');

console.log('Firebase Admin SDK version:', admin.SDK_VERSION);
console.log('Firebase Admin SDK credential:', admin.credential);
