-- Migration Template for Drizzle Kit
-- This template provides a consistent structure for database migrations
-- Generated on: {{ timestamp }}
-- Migration: {{ name }}

-- ========================================
-- UP MIGRATION
-- ========================================

-- Add your schema changes here
-- Example:
-- ALTER TABLE table_name ADD COLUMN column_name data_type;
-- CREATE INDEX index_name ON table_name (column_name);

-- ========================================
-- DOWN MIGRATION (ROLLBACK)
-- ========================================

-- Add rollback statements here
-- Example:
-- ALTER TABLE table_name DROP COLUMN column_name;
-- DROP INDEX IF EXISTS index_name;

-- ========================================
-- DATA MIGRATION (if needed)
-- ========================================

-- Add data migration statements here
-- Example:
-- UPDATE table_name SET column_name = 'default_value' WHERE column_name IS NULL;

-- ========================================
-- VERIFICATION
-- ========================================

-- Add verification queries here
-- Example:
-- SELECT COUNT(*) FROM table_name WHERE column_name IS NOT NULL;
